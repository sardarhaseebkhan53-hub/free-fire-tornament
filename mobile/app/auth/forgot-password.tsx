import {useState} from 'react';import {router} from 'expo-router';import {Text,TextInput,TouchableOpacity,View} from 'react-native';import {auth} from '../../lib/api';import {theme} from '../../theme';export default function ForgotPassword(){const [email,setEmail]=useState('');const [done,setDone]=useState(false);const [error,setError]=useState('');async function submit(){try{await auth.forgot(email);setDone(true)}catch(e){setError(e instanceof Error?e.message:'Unable to submit request.')}}return <View style={{flex:1,backgroundColor:theme.colors.bg,padding:24,justifyContent:'center'}}><Text style={{color:theme.colors.text,fontSize:32,fontWeight:'800'}}>Reset password</Text><Text style={{color:theme.colors.muted,lineHeight:22,marginTop:12}}>Enter your account email. If it exists, ClutchNex will send a secure reset link.</Text><TextInput placeholder="Email address" placeholderTextColor={theme.colors.muted} autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail} style={{backgroundColor:theme.colors.card,color:theme.colors.text,padding:16,borderRadius:10,marginTop:25}}/>{done?<Text style={{color:theme.colors.success,marginTop:15}}>Check your email for the secure reset link.</Text>:error?<Text style={{color:theme.colors.danger,marginTop:15}}>{error}</Text>:null}<TouchableOpacity onPress={submit} style={{backgroundColor:theme.colors.primary,padding:17,borderRadius:10,marginTop:20}}><Text style={{color:'#fff',textAlign:'center',fontWeight:'800'}}>SEND RESET LINK</Text></TouchableOpacity><TouchableOpacity onPress={()=>router.back()}><Text style={{color:theme.colors.muted,textAlign:'center',marginTop:22}}>Back to sign in</Text></TouchableOpacity></View>}
