import {useQuery} from '@tanstack/react-query';import {queries} from '../../lib/api';import {FlatList,Text,View} from 'react-native';import {theme} from '../../theme';export default function Leaderboard(){const q=useQuery({queryKey:['leaderboard'],queryFn:queries.leaderboard});return <View style={{flex:1,backgroundColor:theme.colors.bg,padding:20,paddingTop:60}}><Text style={{color:theme.colors.text,fontSize:28,fontWeight:'800',marginBottom:22}}>Leaderboard</Text><FlatList data={q.data?.items||[]} keyExtractor={(x:any)=>String(x.rank||x.user?.username)} renderItem={({item,index}:any)=><View style={{backgroundColor:theme.colors.card,padding:16,borderRadius:10,marginBottom:8,flexDirection:'row'}}><Text style={{color:theme.colors.primary,fontWeight:'800',width:36}}>{item.rank||index+1}</Text><Text style={{color:theme.colors.text,fontWeight:'700',flex:1}}>{item.user?.username||item.username||'Player'}</Text><Text style={{color:theme.colors.muted}}>{item.totalPoints||item.points||0} pts</Text></View>} ListEmptyComponent={<Text style={{color:theme.colors.muted}}>Rankings appear after verified results.</Text>}/></View>}
