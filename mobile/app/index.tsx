import {Redirect} from 'expo-router';import {useEffect,useState} from 'react';import * as SecureStore from 'expo-secure-store';import {token} from '../lib/api';import {ActivityIndicator,View} from 'react-native';export default function Index(){const [ready,setReady]=useState(false);const [has,setHas]=useState(false);const [seen,setSeen]=useState(false);useEffect(()=>{Promise.all([token(),SecureStore.getItemAsync('clutchnex_onboarding_seen')]).then(([t,s])=>{setHas(!!t);setSeen(!!s);setReady(true)})},[]);if(!ready)return <View style={{flex:1,backgroundColor:'#090A0E',alignItems:'center',justifyContent:'center'}}><ActivityIndicator color="#FF3655"/></View>;if(!seen)return <Redirect href="/onboarding"/>;return <Redirect href={has?'/(tabs)':'/auth/login'}/>}
